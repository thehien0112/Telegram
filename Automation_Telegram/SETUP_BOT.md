# Hướng Dẫn Kích Hoạt "Trợ Lý Thảo Dược" Trên Telegram 🤖

Để nhận bài thuốc hàng ngày, chị cần tạo một con Bot riêng (miễn phí và bảo mật).
Hãy làm theo 3 bước sau nhé:

## Bước 1: Lấy "Chìa Khóa" (API Token) 🔑
1.  Mở Telegram, tìm kiếm từ khóa: `BotFather` (có tích xanh).
2.  Bấm **Start**.
3.  Gõ lệnh: `/newbot`
4.  Đặt tên cho Bot (Ví dụ: `Mẹ Ba Vì Learning`).
5.  Đặt username cho Bot (Phải kết thúc bằng chữ `bot`, ví dụ: `me_bavi_learning_bot`).
6.  BotFather sẽ gửi cho chị một đoạn mã dài (màu đỏ). Đó là **API Token**.
    *   Ví dụ: `718293:AAGHs82...`

## Bước 2: Lấy "Địa Chỉ Nhà" (Chat ID) 🏠
1.  Tìm bot mà chị vừa tạo trên Telegram. Bấm **Start**.
2.  Gõ một tin nhắn bất kỳ cho nó (ví dụ: "Hello").
3.  Tìm bot có tên: `userinfobot` (hoặc `RawDataBot`). Bấm **Start**.
4.  Nó sẽ trả về một dòng con số `Id`. Đó là **Chat ID** của chị.
    *   Ví dụ: `123456789`

## Bước 3: Gửi Cho Em 📩
Chị copy và dán 2 thông tin đó vào đây giúp em nha:
1.  **Token:** ...
2.  **Chat ID:** ...

Sau khi có 2 thông tin này, em sẽ cài đặt để Bot tự động gửi bài thuốc cho chị mỗi sáng! 🌞🌿
